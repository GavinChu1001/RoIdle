export function renderRecommendedScoreGap(entries) {
  if (typeof window.renderRecommendedScoreGap === 'function') return window.renderRecommendedScoreGap(entries);
}
export function renderAdvicePanel(stats) {
  if (typeof window.renderAdvicePanel === 'function') return window.renderAdvicePanel(stats);
}
