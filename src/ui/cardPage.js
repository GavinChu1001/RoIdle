export function renderCards() {
  if (typeof window.renderCards === 'function') return window.renderCards();
}
export function renderBossCardSynthesis() {
  if (typeof window.renderBossCardSynthesis === 'function') return window.renderBossCardSynthesis();
}
