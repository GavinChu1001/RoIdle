{
  "name": "rune-frontier-idle",
  "version": "0.72.1",
  "private": true,
  "description": "RO-like idle web game with lightweight account saves.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node scripts/check.mjs",
    "test": "node scripts/test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
